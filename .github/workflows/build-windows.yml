name: Build Windows Executable

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable
        run: |
          pyinstaller --noconfirm --onefile --windowed --name "TechVaultDeviceDetector" app.py

      - name: Prepare distributable package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package | Out-Null
          Copy-Item dist/TechVaultDeviceDetector.exe package/
          Copy-Item USO-RAPIDO.txt package/
          Compress-Archive -Path package/* -DestinationPath TechVaultDeviceDetector-windows.zip -Force

      - name: Upload artifact (zip)
        uses: actions/upload-artifact@v4
        with:
          name: TechVaultDeviceDetector-windows
          path: TechVaultDeviceDetector-windows.zip

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: windows-latest

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: TechVaultDeviceDetector-windows
          path: dist

      - name: Publish release asset
        uses: softprops/action-gh-release@v2
        with:
          files: dist/TechVaultDeviceDetector-windows.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
